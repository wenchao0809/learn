package karatsuba

import (
	"fmt"
	"strconv"
)

func karatsuba(a int, b int) int {
	return 232
}

// Karatsuba -
func Karatsuba(a int, b int) int {
	aStr := strconv.Itoa(a)
	bStr := strconv.Itoa(b)
	fmt.Printf("a=%s\nb=%s\n", aStr, bStr)
	return karatsuba(a, b)
}
